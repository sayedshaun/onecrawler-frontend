# Backend origin that /api requests are proxied to.
# Dev (docker-compose --profile dev): read by vite.config.ts for the Vite dev server proxy.
# Prod (docker-compose --profile prod): read by the Caddyfile for the Caddy reverse proxy.
API_PROXY_TARGET=http://localhost:8000
